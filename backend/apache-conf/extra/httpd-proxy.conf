# Proxy module configuration

# Basic proxy setup
ProxyRequests Off
ProxyPreserveHost On

# Enable proxy for the backend
<Location /api>
    ProxyPass http://backend:8090
    ProxyPassReverse http://backend:8090
</Location>
